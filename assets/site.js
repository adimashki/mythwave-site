const select=document.querySelector('#region-filter');if(select){select.addEventListener('change',()=>{let count=0;document.querySelectorAll('.relation').forEach(card=>{const show=select.value==='all'||card.querySelector('h3').textContent.includes(select.value);card.hidden=!show;if(show)count++});const status=document.querySelector('.relations p[role="status"]');if(status)status.textContent=count+' diplomatic relationships'});}document.addEventListener('keydown',event=>{if(event.key==='Escape')document.querySelectorAll('.static-mobile-menu[open]').forEach(menu=>menu.removeAttribute('open'))});document.querySelectorAll('.static-mobile-menu a').forEach(link=>link.addEventListener('click',()=>link.closest('details').removeAttribute('open')));